#include "ft_select.h"

void	event_quit(t_env *e)
{
	reset_terminal(e);
}

void	event_left(t_env *e)
{
}

void	event_right(t_env *e)
{

}
void 	event_gesture(t_env *e, char *s)
{
	if (s == "d")
		reset_terminal(e);
}
